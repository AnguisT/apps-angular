import {browser, by, element} from "protractor";
import {waitVisibility, waitVisibilityAndClick} from "../../../app/tests/e2e/common-helper";


export function mediaCases() {
    // it('(e2e) 1. Should get page media', () => {
    //     waitVisibility(element(by.css('media')), (media) => {
    //         expect(media.isDisplayed()).toBe(true);
    //     });
    // });
}
